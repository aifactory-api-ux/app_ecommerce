#  - API Documentation

**Generated by IA-Factory Developer Agent**  
**Date**: 2026-05-14 19:31:10 UTC

---

## Overview

This API has 23 endpoints.

## Endpoints

### `/`

#### 🔴 DELETE

**Defined in**: `backend/app/api/cart.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X DELETE http://localhost:3000/
```

#### 🟢 GET

**Defined in**: `backend/app/api/cart.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X GET http://localhost:3000/
```

#### 🟡 POST

**Defined in**: `backend/app/api/products.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X POST http://localhost:3000/ \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/checkout`

#### 🟡 POST

**Defined in**: `backend/app/api/orders.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X POST http://localhost:3000/checkout \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/health`

#### 🟢 GET

**Defined in**: `backend/app/main.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X GET http://localhost:3000/health
```

---

### `/items`

#### 🟡 POST

**Defined in**: `backend/app/api/cart.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X POST http://localhost:3000/items \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/items/{item_id}`

#### 🔴 DELETE

**Defined in**: `backend/app/api/cart.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X DELETE http://localhost:3000/items/{item_id}
```

#### 🔵 PUT

**Defined in**: `backend/app/api/cart.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X PUT http://localhost:3000/items/{item_id} \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/login`

#### 🟡 POST

**Defined in**: `backend/app/api/auth.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X POST http://localhost:3000/login \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/logout`

#### 🟡 POST

**Defined in**: `backend/app/api/auth.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X POST http://localhost:3000/logout \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/me`

#### 🟢 GET

**Defined in**: `backend/app/api/users.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X GET http://localhost:3000/me
```

---

### `/profile`

#### 🟢 GET

**Defined in**: `backend/app/api/users.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X GET http://localhost:3000/profile
```

#### 🔵 PUT

**Defined in**: `backend/app/api/users.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X PUT http://localhost:3000/profile \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/refresh`

#### 🟡 POST

**Defined in**: `backend/app/api/auth.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X POST http://localhost:3000/refresh \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/register`

#### 🟡 POST

**Defined in**: `backend/app/api/auth.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X POST http://localhost:3000/register \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/sales-summary`

#### 🟢 GET

**Defined in**: `backend/dashboard-api/api/dashboard.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X GET http://localhost:3000/sales-summary
```

---

### `/seed`

#### 🟡 POST

**Defined in**: `backend/app/api/seed.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X POST http://localhost:3000/seed \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/top-products`

#### 🟢 GET

**Defined in**: `backend/dashboard-api/api/dashboard.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X GET http://localhost:3000/top-products
```

---

### `/{order_id}`

#### 🟢 GET

**Defined in**: `backend/app/api/orders.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X GET http://localhost:3000/{order_id}
```

---

### `/{order_id}/cancel`

#### 🟡 POST

**Defined in**: `backend/app/api/orders.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X POST http://localhost:3000/{order_id}/cancel \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/{product_id}`

#### 🔴 DELETE

**Defined in**: `backend/app/api/products.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X DELETE http://localhost:3000/{product_id}
```

#### 🔵 PUT

**Defined in**: `backend/app/api/products.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X PUT http://localhost:3000/{product_id} \
  -H "Content-Type: application/json" \
  -d '{"key": "value"}'
```

---

### `/{slug}`

#### 🟢 GET

**Defined in**: `backend/app/api/products.py`

**Description**: Auto-detected endpoint

**Example Request**:
```bash
curl -X GET http://localhost:3000/{slug}
```

---
